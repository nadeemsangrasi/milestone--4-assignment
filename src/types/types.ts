export interface INavLinks {
  url: string;
  text: string;
}
export interface IBookCard {
  number: number;
  title: string;
  releaseDate: string;
  description: string;
  cover: string;
  pages: number;
}
